spring:
  data:
    mongodb:
      port: 0
      database: testdb

  shell:
    interactive:
      enabled: false
    command:
      version:
        enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.18
